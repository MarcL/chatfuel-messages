import BaseTemplate from '../baseTemplate';

class ShareButton extends BaseTemplate {
    constructor() {
        super();

        this.template = { type: 'element_share' };
    }
}

export default ShareButton;
